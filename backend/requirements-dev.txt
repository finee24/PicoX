# =============================================================================
# Picox — Dipendenze di sviluppo e CI (non installate in produzione)
# =============================================================================
# Installazione:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# --- Test --------------------------------------------------------------------
pytest>=8.0,<9.0
# `asyncio_mode = auto` in pytest.ini: le funzioni async non hanno bisogno di
# essere decorate una per una.
pytest-asyncio>=0.24,<2.0

# --- Lint & type check -------------------------------------------------------
# Stesse versioni usate dagli hook locali e dalla CI: un lint che passa in
# locale e fallisce in CI (o viceversa) è peggio di nessun lint.
ruff>=0.6,<1.0
mypy>=1.11,<2.0
# PyYAML non spedisce annotazioni proprie: senza gli stub, `import yaml` in
# `prompt_loader.py` è un errore di mypy, non un warning.
types-PyYAML>=6.0,<7.0
