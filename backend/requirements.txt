# =============================================================================
# Picox — Backend dependencies (Python 3.11+)
# =============================================================================

# --- Web framework -----------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Config & validation -----------------------------------------------------
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# --- Auth --------------------------------------------------------------------
# Verifica locale del JWT Supabase senza round-trip verso GoTrue.
# L'extra `crypto` è obbligatorio, non opzionale: i progetti con signing key
# asimmetrica usano ES256/RS256/EdDSA, che senza `cryptography` fanno fallire
# `jwt.decode` — cioè l'autenticazione di tutti gli utenti.
PyJWT[crypto]>=2.9,<3.0

# --- Servizi esterni ---------------------------------------------------------
google-genai>=1.0,<2.0
apify-client>=1.8,<3.0
supabase>=2.10,<3.0

# --- HTTP client (download video in streaming) -------------------------------
httpx>=0.27,<1.0
